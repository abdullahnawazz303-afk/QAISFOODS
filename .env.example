# Supabase — lab project defaults (instructor can run npm i && npm run dev immediately)
# Students: replace these with your own keys in .env after npm install if required.
VITE_SUPABASE_URL=https://elfnxgojpdtoqhzreupk.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_gFYLI1a1EMw1FDReobAXLw_tn80NlKd

# Server-side only — not used by the React app (students may include for edge functions)
SUPABASE_SERVICE_ROLE_KEY=your-secret-service-role-key

# Optional flags
# VITE_PRODUCT_REVIEWS_REMOTE=true
# VITE_PUBLIC_REVIEWS_ANON=true
